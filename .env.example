# .env.example
# This is an example file. Copy this to a new file named .env
# and fill in your actual API credentials.
# This .env file should NOT be committed to version control.

# Required: Your API key for the LLM provider (e.g., OpenAI)
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: If you are using a custom API endpoint or a proxy,
# specify the base URL here. If you are using the default
# OpenAI API endpoint, you can leave this blank or comment it out.
OPENAI_API_BASE="YOUR_OPTIONAL_API_BASE_URL_HERE"

# Note: The MODEL_NAME (e.g., "Qwen2.5-32B-Instruct") is currently
# set directly within the Python scripts. If you need to change it,
# you'll need to modify the scripts themselves.
